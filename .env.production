# Sentry Configuration for Production Builds
# NOTE: Removed sensitive auth token. Store `SENTRY_AUTH_TOKEN` in CI secrets or your build environment.
# Example (set as CI secret): SENTRY_AUTH_TOKEN
SENTRY_ORG=egwallet-hu
SENTRY_PROJECT=react-native
NODE_ENV=production
